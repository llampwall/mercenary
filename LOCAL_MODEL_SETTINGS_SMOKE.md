# Local Model Settings Smoke

Smoke test #2 for local-Qwen routing through ALLMIND headless dispatch.
Created by qwen3.6-27b-local via mercenary.run() / claude -p.

Smoke 3 verified — append + commit + push via local Qwen.
